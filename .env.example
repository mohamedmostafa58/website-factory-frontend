# Payload CMS API
PAYLOAD_API_URL=http://localhost:3000
PAYLOAD_API_KEY=

# Webhook secret (must match Payload's WEBHOOK_SECRET)
WEBHOOK_SECRET=dev-webhook-secret

# Cloudflare KV — configured via wrangler.toml for production
# For local dev, wrangler pages dev will use a local KV store
